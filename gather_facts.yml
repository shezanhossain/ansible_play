---
- name: Generate
  hosts: sw
  connection: network_cli
  gather_facts: false
  
  collections:
    - community.network


  tasks:
  - name: Show config
    community.network.exos_command:
      commands: show config vlan
    register: config

  - name: debug 
    debug: 
      var: config.stdout[0]
